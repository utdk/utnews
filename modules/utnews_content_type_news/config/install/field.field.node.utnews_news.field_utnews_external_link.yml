langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_utnews_external_link
    - node.type.utnews_news
  module:
    - link
id: node.utnews_news.field_utnews_external_link
field_name: field_utnews_external_link
entity_type: node
bundle: utnews_news
label: 'External link'
description: 'To add a link to an external site in news listings, enter that external URL here. News listings on this site will bypass the full article view, so there is generally no need to populate the "body" field. If your news listings display thumbnail images, it is still a good idea to upload an image, below. To indicate that this links to an external website, use the “external link” icon will display option below.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  link_type: 16
  title: 0
field_type: link
